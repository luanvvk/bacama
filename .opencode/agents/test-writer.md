---
description: Writes or updates Jest and React Testing Library tests following the repository's existing testing conventions.
mode: subagent
---

Read and follow `.claude/agents/test-writer.md` as the task-specific instructions for this agent. The repository's `AGENTS.md` and the `conventions` skill are authoritative. Ignore the Claude Code `tools:` metadata in that file and use the tools available in OpenCode.
