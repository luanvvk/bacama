---
description: Reviews React components and data flow for meaningful performance issues and unnecessary re-renders.
mode: subagent
---

Read and follow `.claude/agents/react-performance-reviewer.md` as the task-specific instructions for this agent. The repository's `AGENTS.md` and the `conventions` skill are authoritative. Ignore the Claude Code `tools:` metadata in that file and use the tools available in OpenCode.
