---
description: Refactors Tailwind v4 styling to align with the repository's design tokens and component conventions.
mode: subagent
---

Read and follow `.claude/agents/tailwind-refactorer.md` as the task-specific instructions for this agent. The repository's `AGENTS.md` and the `conventions` skill are authoritative. Ignore the Claude Code `tools:` metadata in that file and use the tools available in OpenCode.
